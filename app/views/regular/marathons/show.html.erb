<h1>Participants</h1>

<ul>
  <li><%= link_to "Orphan photos (#{@marathon.photos.orphans.count})", regular_marathon_photos_path(@marathon, only: :orphans) %></li>
  <% @marathon.participants.each do |participant| %>
      <li><%= link_to "Bib: #{participant.bib} (#{participant.photos.count})", regular_marathon_participant_path(@marathon, participant) %></li>
  <% end %>
</ul>